#*-* FunctionAdder *-*

include 'lexer.lets'
include 'parser.lets'

_main_()

def _main_():
	while true :
		input = input('Enter expression: ')
		if input == 'exit'
			exit(0)
		end
		tokens = Lexer(input)
		print parse(tokens)
	end
end